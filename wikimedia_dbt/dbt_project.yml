name: wikimedia_dbt
version: "1.0.0"
config-version: 2

profile: wikimedia_dbt

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  wikimedia_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table